# docker-image-and-container
Docker y MySQL: se descarga la imagen mysql:8.0-debian, se levanta un contenedor con mapeo de puertos, y se conecta DBeaver para crear la base de datos pets_db y una tabla pets con varias mascotas. Finalmente se publica la imagen en DockerHub. 
